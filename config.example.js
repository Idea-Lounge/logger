(function () {
  var config = {
    database: {
      url: <url>,
      port: <port>,
      name: <database-name>,
      username: <username>,
      password: <password>
    }
  };
  
  module.exports = config;
})();
